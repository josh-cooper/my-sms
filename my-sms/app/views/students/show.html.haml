:ruby
  display_attrs = %i[id title first_name middle_name last_name email birth_date gender]
  display_ovrd = {
    birth_date: { value: l(@student.birth_date, format: :long) }
  }

= render partial: 'shared/model_attr_list',
  locals: {model_instance: @student, display_attrs: display_attrs, display_ovrd: display_ovrd}
