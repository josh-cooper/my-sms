- model_class = Note
%div.page-header
  %h1=t '.title',
  default: [:'helpers.titles.new', 'New %{model}'],
  model: model_class.model_name.human.titleize

= render(partial: 'form_new', locals: { notable: @notable_instance })
